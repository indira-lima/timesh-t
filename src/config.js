/* eslint-disable import/prefer-default-export */
const config = {
  db: {
    version: 1,
    name: 'TimeshitDatabase',
  },
  luxon: { locale: 'pt-BR', zone: 'America/Sao_Paulo' },
};

export default config;
